import { SortQuestionsPipe } from './sort-questions.pipe';

describe('SortQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
